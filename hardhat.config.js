require("@nomiclabs/hardhat-ethers");

module.exports = {
  solidity: "0.8.20",
  paths: {
    artifacts: "./client/src/artifacts", // Optional: Send ABI to frontend later
  },
};


module.exports = {
  solidity: "0.8.20",
  paths: {
    artifacts: "./client/src/artifacts", // Optional: Send ABI to frontend later
  },
};
